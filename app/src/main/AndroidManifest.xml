<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.eddiep.android.voting">

    <application
        android:name=".VoteApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:largeHeap="true"
        android:theme="@style/AppTheme">

        <meta-data android:name="com.google.android.geo.API_KEY" android:value="AIzaSyB5iJ3CTyK3h5VpSaNAimnwMU0p7UUemPI"/>

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ElectionActivity"
            android:label="Elections"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity android:name=".ElectionDetailActivity"
            android:label="Election Details"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity android:name=".ElectionMoreActivity"
            android:label="Candidates"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity android:name=".CandidateProfileActivity"
            android:label="Candidates"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>
